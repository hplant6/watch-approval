[project]
name = "watch-approval-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aioapns>=3.1",
    "fastapi>=0.139.0",
    "pydantic>=2.13.4",
    "uvicorn>=0.50.0",
]
